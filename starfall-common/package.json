{
  "name": "starfall-common",
  "version": "1.0.0",
  "private": true,
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc --build -f && cp -r proto dist",
    "prepare": "tsc --build && cp -r proto dist",
    "clean": "tsc --build --clean"
  },
  "devDependencies": {
    "@types/ramda": "^0.30.2",
    "@types/node": "^22.7.5",
    "@types/ecef-projector": "file:Types/ecef-projector",
    "@types/google-protobuf": "^3.15.2",
    "ts-protoc-gen": "^0.15.0",
    "typescript": "~5.4.0"
  },
  "dependencies": {
    "ramda": "^0.30.1",
    "date-fns": "^2.29.2",
    "date-fns-tz": "^1.3.6",
    "ecef-projector": "^1.0.1",
    "jsonschema": "github:tdegrunt/jsonschema#84ed77b",
    "uuid": "^8.3.2"
  }
}
